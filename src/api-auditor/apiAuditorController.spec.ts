import { apiAuditorController } from "./apiAuditorController";
import { apiAuditorController_copy } from "./apiAuditorController_copy";
describe('API Auditor Controller', () => {

    test("Fetches successfully data from an API", async () => {

        const filepath = "./src/data/customer.edmx" ;
        // await apiAuditorController_copy(filepath);
        // await apiAuditorController(filepath);

    });    
    
    test("Test CSV file import", async () => {

  /*       await apiAuditortest();

        // const filepath = '../../data.csv';
        const filepath = path.join(__dirname, 'data.csv');
        await loadCsvFile(`file:///${filepath}`);
        console.log('done'); */
    });   
    

});